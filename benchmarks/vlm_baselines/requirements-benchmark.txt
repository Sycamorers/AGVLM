# Benchmark-only additions. Install on top of the repository environment.
# The main pyproject already pins torch, transformers, accelerate, bitsandbytes,
# Pillow, PyYAML, numpy, pandas, and scikit-learn for the full project env.
qwen-vl-utils>=0.0.8
molmo_utils
decord2; platform_system == "Linux"

# Optional memory/performance fallback for compatible GPUs and model kernels.
flash-attn>=2.8.0.post2; platform_system == "Linux"
